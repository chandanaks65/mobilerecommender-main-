Mobile Recommender System

A simple machine-learning–based mobile phone recommendation system built using Streamlit, Pandas, Scikit-Learn, and Seaborn.

1. Clone the repository

git clone https://github.com/chandanaks65/mobilerecommender-main-.git

2. Navigate into the project directory

cd MobileRecommenderSystem-main

3. Create a virtual environment and activate it

pip install virtualenv
virtualenv movieVenv
movieVenv\Scripts\activate.ps1

4. Install the required dependencies

pip install -r requirements.txt

5. Run the Streamlit application

streamlit run app.py


